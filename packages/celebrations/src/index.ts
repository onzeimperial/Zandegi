/**
 * @zandegi/celebrations
 *
 * Pre-generated celebration video clips (Seedance) and the pure logic that
 * picks among them for a completion moment. Not wired to any live completion
 * event yet — packages/economy, packages/db, and packages/api don't exist as
 * runnable infrastructure (see docs/BUILD-LOG.md). See GENERATING.md for how
 * new clips are produced and added to library/manifest.json.
 */

export * from "./types";
export * from "./prompt";
export * from "./select";
