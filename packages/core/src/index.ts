/**
 * @zandegi/core
 *
 * Pure domain logic: types, guards, deterministic scoring primitives. NO I/O, NO framework imports (CLAUDE.md §4).
 *
 * Scaffold only — no implementation yet.
 */
export {};
